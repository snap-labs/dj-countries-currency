============
Installation
============

At the command line::

    $ easy_install dj-countries-currency

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv dj-countries-currency
    $ pip install dj-countries-currency
